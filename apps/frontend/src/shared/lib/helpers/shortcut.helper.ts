import { CTRL, ENTER, ESCAPE, NUMPAD_ENTER } from '@/shared/const/keyboardKeys'

export const CREATE_AND_OPEN = `${CTRL}+${ENTER}`
export const CREATE = [ENTER, NUMPAD_ENTER]
export const UPDATE = [ENTER, NUMPAD_ENTER]
export const CANCEL = ESCAPE
