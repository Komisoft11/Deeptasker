export type ProjectRoleType = 'admin' | 'controller' | 'assigner' | 'user' | 'guest'

export enum ProjectRoleEnum {
  admin = 'admin',
  controller = 'controller',
  assigner = 'assigner',
  user = 'user',
  guest = 'guest'
}
